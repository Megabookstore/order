pipeline {
    agent any()

    triggers {
        // 해당 조건의 상황이 발생할 때마다 pipeline을 실행하겠다.
    }

    environment {
        // 환경설정 ex) AWS ACCESS KEY, SECRET, REGION 등을 지정
    }

    stages {
        stage('Prepare') {
            // switch-case문처럼 조건에 맞는 stage를 실행한다.
        }
    }
}화